{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "test ! -f \"${TMPDIR:-/tmp}/discuss-$CLAUDE_CODE_SESSION_ID.lock\" || { echo 'Discussion mode is active — file edits are blocked. Run /discuss end to exit.'; exit 2; }"
          }
        ]
      }
    ]
  }
}
